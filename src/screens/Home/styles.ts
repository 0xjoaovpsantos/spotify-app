import styled from 'styled-components/native';
import AppColor from '../../utils/AppColors';

export const Container = styled.View`
  flex: 1;
  background-color: ${AppColor.backgroundScreen};
  padding: 16px;
`;
